apiVersion: v1
kind: Service
metadata:
  name: cigithubaction
spec:
  type: LoadBalancer
  selector:
    app: cigithubaction
  ports:
  - protocol: TCP
    port: 18080
    targetPort: 18080
    nodePort: 8080
    name: http
